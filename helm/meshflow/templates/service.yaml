apiVersion: v1
kind: Service
metadata:
  name: {{ include "meshflow.fullname" . }}
  labels:
    {{- include "meshflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meshflow.selectorLabels" . | nindent 4 }}
